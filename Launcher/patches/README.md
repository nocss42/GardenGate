# Patches

Each .xdelta file in this folder reverts them to their pre eaac counterparts when applied. gw1 uses the latest version, so it doesn't need patches.

gw1 = `1.0.3.0`
gw2 = `1.0.12`
bfn = `1.0.55.50001`
